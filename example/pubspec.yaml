name: example
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  cupertino_icons: ^1.0.6
  flutter:
    sdk: flutter
  flutter_hooks: ^0.20.5
  smart_events_handler:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  leancode_lint: ^11.0.0

flutter:
  uses-material-design: true
